[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm mcp:translation:build"

[deploy]
startCommand = "pnpm mcp:translation:start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
numReplicas = 1
